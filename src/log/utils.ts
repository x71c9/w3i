/**
 *
 * Log utils module
 *
 * @packageDocumentation
 *
 */

// import * as types from '../types/index.js';
// import {config} from '../config/config.js';

// export function _is_traceble(log_level?: types.LogLevel): boolean {
//   const level = log_level || config.log_level;
//   if (level === types.LOG_LEVEL.TRACE) {
//     return true;
//   }
//   return false;
// }

// export function _is_debugable(log_level?: types.LogLevel): boolean {
//   const level = log_level || config.log_level;
//   if (_is_traceble(level) || level === types.LOG_LEVEL.DEBUG) {
//     return true;
//   }
//   return false;
// }

// export function _is_infoble(log_level?: types.LogLevel): boolean {
//   const level = log_level || config.log_level;
//   if (_is_debugable(level) || level === types.LOG_LEVEL.INFO) {
//     return true;
//   }
//   return false;
// }

// export function _is_warnable(log_level?: types.LogLevel): boolean {
//   const level = log_level || config.log_level;
//   if (_is_infoble(level) || level === types.LOG_LEVEL.WARN) {
//     return true;
//   }
//   return false;
// }

// export function _is_errable(log_level?: types.LogLevel): boolean {
//   const level = log_level || config.log_level;
//   if (_is_warnable(level) || level === types.LOG_LEVEL.ERROR) {
//     return true;
//   }
//   return false;
// }
